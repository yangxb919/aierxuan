# 两周工作回顾 + 未提交改动分批入库 + 健康检查

日期：2026-05-19 18:31
关联：上一次入库收尾在 5/9 (fe121d6)，5/12 ~ 5/15 一直在本地工作树堆改动。

## 1. 未提交改动按主题入库（8 个 commit，未 push）

```
4d738aa docs(操作日志): 补全 5/6 ~ 5/15 操作日志（17 篇）
ad3c92c docs(content): Week3 笔记本选题筛选 + NotebookLM 分析 + Excel 内容计划
9cc5bd0 chore(ops): 新增 R2 增量备份脚本 scripts/backup-uploads-to-r2.sh
eda2f7a feat(products): 接入新 schema (locale/title/short_desc) + JsonLd 抽出 + verified-20260514 同步
be47731 feat(seo): 抽出 brand-facts + technical-seo，全站统一品牌事实与结构化数据
cfa9c00 refactor(html): root layout 改用静态 lang="en"，移除 cookies() 依赖
56fc657 chore(infra): /uploads 走 1 年不可变缓存 + Supabase 兼容新旧 key 命名
e6da5c9 chore(gitignore): 扩展忽略覆盖 GA/GSC/GTM 抓取与本地大文件
```

`.gitignore` 新增挡掉：根目录 ga-*.md / gsc-*.md / gtm-*.md 抓取产物、`.agents/`、`tmp-302ai-docs/`、`hyperframes-aierxuan-intro/`（149MB）、`网站图片和核对后文档_*/`（222MB）、Handbook docx（32MB）、`操作日志/backups/`（3.5MB DB 快照 JSON）。

## 2. 本地构建：✅ 通过

`npm run build` exit 0，路由树完整（含 [lang] SSG 6 语言 × 多页 + admin/api SSR）。

## 3. 线上健康检查（生产 www.aierxuanlaptop.com）

| 项 | 结果 | 备注 |
|---|---|---|
| EN/RU 首页 200 | ✅ | private, no-cache, no-store + BYPASS（HTML 走源 + 后端控制 cache） |
| /admin 307 + BYPASS | ✅ | 重定向到登录，敏感路径不缓存 |
| /api/admin 404 + BYPASS | ✅ | 期望（无 GET handler） |
| /sitemap.xml & /robots.txt 200 | ✅ |  |
| GSC 验证 HTML 200 | ✅ | `/googlece505444d2b1a7fb.html` 可访问，proxy matcher 已放行 |
| **apex `aierxuanlaptop.com/` 应 301 → 实测 404** | ❌ **回归** | CF 边缘 BYPASS 404；说明请求到了源但 nginx 没有 server_name 匹配 apex（或 CF apex 解析未正确指向 www-only origin）。proxy.ts 里 301 代码还在，但根本没机会执行。 |
| `/uploads/...` 应 1y immutable → 实测 30d，无 immutable | ⚠️ **次要回归** | next.config 的 `headers()` 只对 Next.js 路由生效，**对 `public/uploads/` 静态文件不生效**——这些文件直接由 nginx 读盘返回（已 HIT）。要 1y immutable 必须改 nginx。本次 commit 56fc657 的 next.config 改动是对的但兜底意义大于实际效果。 |
| HTML 响应 cache-control 重复两条 | ⚠️ 冗余 | nginx 和 Next.js 都在加，叠加；功能不影响，规范客户端取第一条。 |

## 4. push 已完成

`git push origin main`：`2f7e3bf..4d738aa  main -> main`，8 个 commit 已同步。
push 前用 service_role 直查 Supabase 确认 `product_translations` 实际列已经是
`locale / title / short_desc / long_desc / key_specs / seo_title / seo_desc`，
旧的 `language_code / name / short_description` 已被删除。eda2f7a 正是修
线上"产品页一直显示 slug"回归（旧代码读不到已删的 language_code，translation
永远 undefined → fallback 到 `[0].name` 也读不到 → 最终落到 product.slug）。

⚠️ `src/types/database.ts` 仍是旧 schema（technical debt），下次新功能开发前
应跑 `supabase gen types` 重新生成。

⚠️ push 本身**不触发线上部署**（部署是 VPS 手动 build + PM2 reload）。
下次手动部署时这套代码会上线，会同时修复产品页 slug 显示回归。

## 5. CF Redirect Rule ✅ 已上线

用户在自己 Chrome 手动创建（CDP 自动化失败：CF dashboard 升级反爬，splash 永远
卡在 50%；用户已确认这是规则的）。

- 规则名：`apex-to-www-301`（顺序 1，活动，1/10 使用）
- 匹配：主机名 等于 `aierxuanlaptop.com`
- 操作：301 Dynamic Redirect → `concat("https://www.aierxuanlaptop.com", http.request.uri.path)`，preserve query

验证结果（2026-05-19 15:37 UTC）：

| 测试 | 结果 |
|---|---|
| `aierxuanlaptop.com/` | 301 → `https://www.aierxuanlaptop.com/` |
| `aierxuanlaptop.com/en` | 301 → `https://www.aierxuanlaptop.com/en` |
| `aierxuanlaptop.com/products?foo=bar` | 301 → `https://www.aierxuanlaptop.com/products?foo=bar` |
| Follow-redirect 端到端 | 1 跳 → 200 |

`server: cloudflare` 边缘 301，0 源负载。

### web-access skill 踩坑（已记入 site-patterns）
CF dashboard 在 CDP 监控下：navigator.webdriver=false 不暴露，HTML 740KB 但
React 不向 body 写文本（innerText.length=0）。第一次 OAuth → home/overview 偶然
能渲染，任何后续 navigate 触发 hard reload 后 splash 永远卡 50% 不动。已写
`~/.claude/skills/web-access/references/site-patterns/dash.cloudflare.com.md`
让后续 skill 加载时直接看到结论，避免再硬试。

## 6. /uploads 真 1y immutable（缓做）

CF 已 HIT 30 天，性价比不高，等下次 VPS 重启加载 kernel 时顺手在
`/etc/nginx/sites-enabled/aierxuanlaptop.com` 加：
```
location ~ ^/(images|uploads)/ {
  add_header Cache-Control "public, max-age=31536000, immutable";
}
```
然后 `nginx -t && nginx -s reload`。

## 7. 用户待办（沿用 5/9）
- GitHub Settings → 重启 main 分支保护规则
- 选窗口让我远程重启 VPS 加载 kernel 6.8.0-1053-aws
- ~~CF Single Redirect~~ ✅ 已上线
- 选窗口让我远程改 nginx 加 /images/ + /uploads/ 1y immutable header
- （可选）建一个 CF API Token（Account Rulesets:Edit + Zone:Read，限
  aierxuanlaptop.com zone），存到 ~/.config/aierxuan/cf.env，下次 CF 操作
  不再需要碰 dashboard

## 5. 用户待办（沿用 5/9）
- GitHub Settings → 重启 main 分支保护规则
- 选窗口让我远程重启 VPS 加载 kernel 6.8.0-1053-aws
