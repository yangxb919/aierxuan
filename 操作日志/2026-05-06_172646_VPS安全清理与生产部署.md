# VPS 安全清理 + 生产部署

- 日期：2026-05-06 17:26
- VPS：aierxuan (AWS aarch64 / Ubuntu)
- 状态：✅ 全部完成，线上 200 OK

## 安全清理（处理 2025-12-09 入侵残留）

清理三处遗留后门 + 注入：
- `/etc/init.d/rondo` + `/etc/rc?.d/*rondo*`（SysV 启动钩子）
- `/usr/bin/softirq`（伪装 ksoftirqd 的 ARM64 ELF，8MB）
- `aierxuan-website/ecosystem.config.js` 末尾 5 行 `xorDecode` → `li1.pics/h/go.js` 注入

深度验证（无 rootkit）：
- `/etc/ld.so.preload` 不存在 ✅
- 内核模块 33 个全部合法 ✅
- `/usr/lib` 下无可疑 .so ✅
- 无连接挖矿池 ✅

备份：`/root/security-cleanup-2026-05-06/`

## 生产部署

本地新增 commit `ac3fe63 feat(analytics): 添加 GTM 容器`（cherry-pick VPS 独有改动），
连同前 6 个 commit 一起推送：

```
ac3fe63 feat(analytics): GTM 容器
b67b602 docs(marketing): 广告投放方案
3b7ebf5 docs: 项目文档与操作日志
d4d9838 feat(seo): 非 www → www 301 重定向
fdf218a feat(ads): Google Ads 转化追踪
7c70466 feat(ui): 全站深色科技风重设计
7da2abe chore: 添加根 .gitignore
```

VPS 部署：`git pull` (59 文件) → `npm ci` (18s) → `npm run build` (30s) → `pm2 reload`

健康检查全部 HTTP 200：
- https://www.aierxuanlaptop.com/  → 203ms
- /en → 366ms / /ru → 65ms / /health → 85ms

部署备份：`/root/deploy-backup-20260506-092334/`

## 待办

- `.env.local` 配置 `NEXT_PUBLIC_GOOGLE_ADS_ID` 与 `NEXT_PUBLIC_GOOGLE_ADS_LEAD_SEND_TO` 后重启
- 建议轮换 Supabase service role key 等历史敏感凭据（入侵期间可能已泄露）
- 可选清理 next.config.mjs 中过时的 eslint 配置（Next 16 警告）
